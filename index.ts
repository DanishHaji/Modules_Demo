/*

export default add    // (export = to make module ) (default = to make module default if its important) (add = name of function or any variable)
function add (num1:number , num2:number) {
        return num1 + num2
};

export let userNames:string[] = ["Ali","Usman", "Danish"] // (Direct export with code)

// export {userName , favColor}; // (Direct export with code) (we can also write it like this to avoid time waste and space.)

export let favColor = "Red" // (Direct export with code)

export let myBio = {  // (Direct export with code)
    name:"Danish",
    age:32,
    gender:"Male",
};

*/

